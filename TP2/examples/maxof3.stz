Int a;
Int b;
Int c;
HICODE
  # Ler o valor para as três variaveis.
  LEARNTHZ a; 
  LEARNTHZ b; 
  LEARNTHZ c;

  ?(a >= b) {
    ?(a >= c) { VISIVEL a; }_{ VISIVEL c; }
  }_{
    ?(b >= c) { VISIVEL b; }_{ VISIVEL c; }
  }
KTHKBYE
