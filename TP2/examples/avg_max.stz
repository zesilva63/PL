Int i;
Int j;
Int avg;
Int max;
Int matrix[2][2];

HICODE

$(i < 2) {
  j = 0;

  $(j < 2) {
    LEARNTHZ matrix[i][j];

    avg = avg + matrix[i][j];
    ?(matrix[i][j] > max) {
      max = matrix[i][j];
    }

    j = j + 1;
  }

  i = i + 1;
}
  avg = avg / (i*j);
  VISIVEL "Media e: ";
  VISIVEL avg;
  VISIVEL "\n";
  VISIVEL "Maximo e: ";
  VISIVEL max;
  VISIVEL "\n";

KTHKBYE
